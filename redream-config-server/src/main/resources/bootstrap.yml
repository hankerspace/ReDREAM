server.port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://bitbucket.org/ciadlabfr/redream-configuration.git
          username: xxxxxx
          password: xxxxxx
          # FIXME : use correct GIT repo & username / password for config server